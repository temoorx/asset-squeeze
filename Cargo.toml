[package]
name = "asset-squeeze"
version = "0.7.0"
edition = "2024"
description = "A lossless-first Flutter, React Native, and web asset optimizer that keeps every asset in its original format."
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
oxipng = { version = "10.1", default-features = false, features = ["parallel"] }
roxmltree = "0.20"
regex = "1.11"
serde_yaml_ng = "0.10"
tempfile = "3.14"

[dev-dependencies]
tempfile = "3.14"
